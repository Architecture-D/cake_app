</div>
<div class="top">
	<div class="cake-image">
	 <%= video_tag("/videos/cake.video.mp4", autoplay: :true, muted: :true, class: 'video-content') %>
	</div><br><br>
 	<div class="home-text"><br><br>
	  <p>ようこそ、NaganoCakeへ！　<br><br>
	    このサイトは、ケーキ販売のECサイトになります。<br><br>
		会員でない方も商品の閲覧はできますが、<br><br>
		購入には会員登録が必要になります。
	 </p><br><br>
	</div>
<div class="container">
	<div class="row"><br>
        <h3><i class="fas fa-birthday-cake"></i>オススメ商品</h3><br>
          <% @products.shuffle.first(4).each do |product| %>
            <div class=col-xs-3>
             <%= link_to product_path(product) do %>
             <%= attachment_image_tag product, :image, :fill, 280, 280, class: 'product-image'%>
             <% end %>
             <%= product.name %><br>
             ¥<%= product.price %>
             </div>
          <% end %>
             <%= link_to "もっとみる >>", products_path,style: "float: right" %>
    </div><br><br>

	<div class="row">
     <div class="col-xs-12">
      <table class="table table-bordered">
       <tr>
        <th class="col-xs-2"><h4>　<i class="fas fa-search"></i>ジャンル別一覧</h4></th>
        <td class="col-xs-10">
    　    <%= link_to("全商品", products_path, {class: "nav-link"}) %>
          <% @genres.each do |genre| %>
          <%= link_to( genre.name, products_path(genre_id: genre.id), {class: "nav-link"}) %>
          <% end %>
          <span class="new-genre"><i class="far fa-star"></i>新ジャンル随時追加予定!!</span>
        </td>
       </tr>
      </table>
	 </div>
	</div>

</div>
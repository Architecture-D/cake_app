<div class="login">
<div class="container">

  <div class="row">
    <br><br><br><br><br><br>
  </div>

  <div class="row">

      <div class="col-xs-4">
      </div>

      <div class="col-xs-4">
        <h3>会員の方はこちらから</h3><br>
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="field">
            <%= f.email_field :email, autofocus: true, autocomplete: "email",class:'form-control',placeholder: "メールアドレス"%><br>
          </div>
          <div class="field">
            <%= f.password_field :password, autocomplete: "current-password",class:'form-control',placeholder: "パスワード" %>
          </div>
          <div class="forgot"><%= render "devise/shared/links" %></div><br>
          <div class="actions">
            <%= f.submit "ログイン",class:"btn btn-block btn-primary" %>
          </div><br>
          <% end %>
      </div>
      <div class="col-xs-4">
      </div>
  </div>
  <div class="row">

      <div class="col-xs-4">
      </div>

      <div class="col-xs-4">
        <h4>登録がお済みでない方</h4>
        <p><%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください</p>
      </div>

      <div class="col-xs-4">
      </div>

  </div>
</div>
</div>
